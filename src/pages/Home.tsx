import PageWrapper from "../components/PageWrapper";
import PetList from "../app/pets/PetList";

const Home = () => {
  return (
    <PageWrapper>
      <PetList />
    </PageWrapper>
  );
};

export default Home;
